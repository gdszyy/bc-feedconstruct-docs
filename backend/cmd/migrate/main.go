// Package main is the migration runner.
//
// BDD placeholder — actual migration logic is added after BDD confirmation.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stdout, "migrate: BDD scaffold; no migrations applied yet")
}
