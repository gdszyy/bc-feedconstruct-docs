// Package config loads and validates environment variables for the BFF.
//
// BDD scaffold — implementation follows after the BDD empty tests
// (config_test.go) are confirmed by the user.
package config
